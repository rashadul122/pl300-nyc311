G0 SIGNED | 2026-09-22 | lead | SEALED OK
ENGINE READY | 2026-09-23 | engine commit 4945864238e3 | run_tests: 164 green | CLI self-test: pass
VENDOR | 2026-09-23 | tools/reconcile from northledger-core | pbi.py sha256 2d119cf4737eb6aff5e8eadac4085fcf3e9731e9b3b37d6e9f316747abb79ff8 | tests 30/30

<!-- G0 = PROCESS.md §P1 steps 1-4. Checked: tools/check_sealed.sh prints SEALED OK on the empty repo; red-tested in a scratch repo (engine path in working copy, force-staged PROCESS.md, stray _design file) and the pre-commit hook blocked a bad commit. The staged-first-commit check runs automatically through the pre-commit hook (git config core.hooksPath tools/hooks). The owner's EXPOSURE record goes below this line, before any build step. -->
PLAN CHANGE | 2026-09-23 | owner: AI builds v1 (answer key unsealed for the builders); card label: AI-built, owner-directed
DECISION | 2026-09-23 | Requests Daily, no Manage aggregations (SPEC §5.1, 2.3.3) | Desktop's Manage aggregations (user-defined aggregations) only redirects queries when the detail table it summarises is in DirectQuery storage mode; in v1 every table, Requests included, is import, so an aggregation mapping on Requests Daily would never be used. Requests Daily therefore stays a plain imported table: its measure Requests (Daily) is shown through C1 (equality with Requests at its grain) and the §11 query-time comparison, and no v1 visual uses it. Routing visuals through it automatically needs an aggregation-aware measure, which is v2 (SPEC §13.2).

<!-- Evidence lines still to be made in the Power BI service; each stays [unrun] until it has been run there and its screenshot or export is in docs/evidence/. -->
SPIKE 1 | [unrun] | Key Influencers (Plan A', numeric Breach Flag) before and after Time Calc (SPEC §8)
SPIKE 2 | [unrun] | Explain the increase on the RLS-free copy _scratch-insights-no-rls (SPEC §8)
SPIKE 3 | [unrun] | Decomposition tree AI (high value) splits on Requests, page 5 (SPEC §8)
PERF | [unrun] | Performance Analyzer, page 3 agency line, Open Backlog EOP vs (Fast), three runs dev and full: visual ms, DAX ms (SPEC §11)
REFRESH PROJECTION | [unrun] | projected full refresh = 25 x median dev refresh of Requests x 4 (SPEC §11)
REQUESTS DAILY SWITCH | [unrun] | keep or switch Requests Daily after the refresh projection (SPEC §11)
DUPLICATE FORK | [unrun] | reason the debug duplicate of Requests was made and deleted (SPEC §11)
WEATHER CHECK | 2026-09-25 dev: 730 of 730, pass (docs/evidence/refresh-3-dev.md) | after each refresh, in DAX query view: EVALUATE ROW ( "Weather days in window", CALCULATE ( COUNTROWS ( 'Date' ), NOT ISBLANK ( 'Date'[Precip mm] ), 'Date'[In Data Window] = TRUE () ) ) must return 730; less means Weather's try ... otherwise fallback fired (the refresh still succeeds) and Precip Bucket reads Unknown on the missing days
KEY INFLUENCERS TYPE | [unrun] | after the first successful refresh, in the service editor: p05_keyinfluencers, Format visual > Analysis > Analysis type = Continuous, save, then commit from the workspace Source control pane and pull before any local edit. visual.json saves no analysis type, and Microsoft Learn says categorical analysis is not supported when discourageImplicitMeasures is true; the property is left for the service to write, not guessed by hand
CI | 2026-09-24 | first push without .github/workflows/sealing.yml: the GitHub login lacks the 'workflow' permission. To add it: gh auth refresh -h github.com -s workflow (approve in the browser), then git add .github && git commit -m 'Add the sealing CI workflow' && git push. The pre-push hook (tools/check_sealed.sh) guards every push meanwhile.
FIX | 2026-09-24 | sourceColumn quotes (refresh 1: "The ''Channel Group'' column does not exist in the rowset") | 80 of 123 sourceColumn values were written 'Quoted Name'. In TMDL, single quotes are name syntax only on references (column, sortByColumn, fromColumn, toColumn, queryGroup); a text property keeps them, so TOM stored "'Channel Group'" (Microsoft Learn, TMDL overview). Quotes stripped; the 111 M-partition sourceColumns now equal the columns of their queries' last steps. New tools/check_tmdl_text.py fails a quoted text property and a sourceColumn its partition never names: 160 problems on ba2ee25, 0 now (engine test test_check_tmdl_text.py; a step in .github/workflows/sealing.yml runs it).
FIX | 2026-09-24 | Weather description | for pWeatherLat/pWeatherLon 40.7812/-73.9665 the Open-Meteo archive answers with grid cell 40.81 N, 74.02 W (New Jersey side); the expression's description now says so. Description only: no query, URL, parameter or value changed.
FIX | 2026-09-25 | Weather Accept-Encoding (refresh 2: "We're sorry, an error occurred during evaluation.;Found invalid data while decoding" x11, raised by the IDbCommand interface) | offered "gzip, deflate" (the service's default), archive-api.open-meteo.com answers Content-Encoding: deflate in zlib form (bytes 78 9c, RFC 1950), which the service's HTTP stack reads as raw deflate and cannot decode. The failure is raised below M, so Weather's try ... otherwise null never caught it and the whole refresh failed on Date. Measured in Power Query Online on the model's own queries: Date failed; the same probe failed without a header and returned 988 days with Headers = [#"Accept-Encoding" = "gzip"]. All 25 dev .csv.gz files decoded there (12,259 rows for 2024-09; carry-in 8,037). Weather now asks for gzip only and reads the response through Binary.Buffer inside try. check_model_fidelity ALLOWED ("M", "Weather") records the three token changes.
FIX | 2026-09-25 | check queries C1, C4, C4r exclude Date's blank member | open requests have no Closed Date, so the inactive Requests[Closed Date] key is BLANK and Date gains its blank member (SPEC §5.3 names this exception). The constant "Check Row" makes SUMMARIZECOLUMNS return every key, so the first run of C1 gave 48 extra rows and C4/C4r 6 each, all on the blank member with every measure BLANK; in C1 they also shared C1's blank grand-total keys. Each query now filters NOT ISBLANK on its Date column; SPEC §9.2 carries the same text. Expected files unchanged; the model unchanged.
RESULT | 2026-09-25 | dev profile reconciles in the Power BI service | refresh 3 (commit c6d461b) succeeded 00:39:57 ET; reconcile PASS 7,338/7,338 cells (C1-C5, C5b, L) and C4r 648/648; grids taken with the Execute Queries REST API, SHA-256 matched page to disk (docs/evidence/refresh-3-dev.md)
